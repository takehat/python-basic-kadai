# 商品の計算結果を返す関数を定義
def calculate(price, tax_rate):

  # 与えられた引数price、tax_rateを考慮した計算結果を、変数totalに代入
  total = price * (1 + 0.01 * tax_rate)

  # 変数totalの値を出力する
  print(f"計算結果は{total}円です。")

# 関数calculateを呼び出す
calculate(1200, 10)